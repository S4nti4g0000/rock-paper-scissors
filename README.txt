Pseudocode for getComputerChoice:

1 Create the function
2 write a variable to store the random number
3 Limit math.random to output three possibilities
4 Create a variable for the returned choice
5 run the random one through an if
6 if 0: rock, if 1: paper, if 2: scissors

Pseudocode for getHumanChoice:

1 Create the function
2 use a variable to store the user prompt
3 add a variable to store the decision as a number instead of a string
4 run the choice through an if to determine if choice was rock = 0, paper = 1 or scissors = 2